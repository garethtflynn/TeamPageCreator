// const intern = require('../lib/intern')
const Intern = require('../lib/intern')

describe('Intern', () => {
    it('Should return an object of name, ID, email, and school ', () => {
        const engTest = new Intern (Gareth, 7, Email, gatech ) 
        expect(empTest).toEqual({'name': 'Gareth', 'id': 7, 'email': 'Email', 'school': 'gatech'})
    }
    )
})
